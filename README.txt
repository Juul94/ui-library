To make it all work, you need to download the node_modules folder:

1) Open the terminal in Visual Studio code and type: npm install

2) Now you can run the commands in the terminal in this order:
   - npm run build
   - npm run serve